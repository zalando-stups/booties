#

debug: true

logging:
  level:
    org.zalando.spring.boot: debug

async:
  executor:
    core-pool-size: 30
    max-pool-size: 45
    queue-capacity: 350
scheduling:
  executor:
    core-pool-size: 12
    max-pool-size: 20